import { ExpenseLIstType } from "./expense-list-prop";
import { SortOrder } from "./sortOrderTypes";

export interface ExpenseListsResponse {
  data: ExpenseLIstType[];
  isLoading: boolean;
  error: Error | null;
  limit: number;
  offset: number;
  sortOrder: SortOrder;
  total: number;
}
